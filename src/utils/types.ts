export type RelatedCourseType = {
    id: number,
    name: string,
    slug: string,
    cover: string
}

export type RelatedArticleType = {
    id: number,
    title: string,
    slug: string,
    cover: string
}